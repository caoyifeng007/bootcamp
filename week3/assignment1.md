ERC721URIStorage contract provides the functionality that token uri can be stored on-chain.

ERC721Pausable contract provides the functionality that we can prevent trading manually for some scenarios.